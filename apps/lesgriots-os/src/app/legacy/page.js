// Ancien dashboard SPA (Dashboard.jsx ~6800 lignes).
// Conservé sur /legacy le temps de la transition vers Mission Control (page d'accueil).
// À supprimer dès que la nouvelle home + les pages modulaires couvrent l'usage.
import Dashboard from '@/components/Dashboard';

export default function Legacy() {
  return <Dashboard />;
}
