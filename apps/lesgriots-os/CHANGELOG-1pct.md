# CHANGELOG 1% — LES GRIOTS OS

Journal des micro-améliorations quotidiennes (Atomic Habits).
Format : `YYYY-MM-DD — [statut] Titre — fichier(s)`

---

- 2026-04-27 — [implémenté] Sécuriser POST /api/expenses (try/catch + transaction INSERT+UPDATE bdc_count + validation minimale + mapping codes SQLite vers VALIDATION_ERROR/FK_VIOLATION/DUPLICATE_ID) — `src/app/api/expenses/route.js`
- 2026-04-28 — [implémenté] Pré-indexer les collections par project_id dans GET /api/data (O(P×N) → O(P+N), ~8× plus rapide en bench 50 projets / 500 exp / 300 tasks, 0 mismatch sur l'output) — `src/app/api/data/route.js`
- 2026-04-29 — [proposé] Ancrer la regex de parsing du cookie `griot_session` dans `getTokenFromRequest()` pour éviter qu'un cookie tiers à suffixe `_griot_session` soit interprété comme le jeton de session — `src/lib/auth.js`
- 2026-04-30 — [implémenté] Purger paresseusement les sessions expirées et invitations périmées/utilisées au moment de la création d'une nouvelle session (2 DELETE dans `createSession()`), pour empêcher la croissance illimitée de `sessions_auth` et `invitations` sans cron ni hook supplémentaire — `src/lib/auth.js`
- 2026-05-02 — [proposé] Sécuriser POST /api/projects (try/catch + transaction INSERT projects + UPDATE next_indices + INSERT expenses par défaut + validation id/code/name/pillar + mapping codes SQLite vers FK_VIOLATION/VALIDATION_ERROR/DUPLICATE_ID, miroir du pattern validé pour /api/expenses Apr 27) — `src/app/api/projects/route.js`
- 2026-05-04 — [proposé] Extraire un helper local `safeJSON(raw, fallback)` dans GET /api/data pour remplacer 5 IIFE try/catch dupliquées (creative_brief, project_journal, ppm_phases, task_phase_validations, providers.categories) — DX, zéro changement de comportement, prépare un futur log centralisé des JSON corrompus — `src/app/api/data/route.js`
- 2026-05-07 — [proposé] Corriger la génération auto de `code_interne` dans POST /api/sessions : remplacer `COUNT(*) FROM sessions` (toutes années confondues) par `MAX(suffix)` sur l'année en cours via `WHERE code_interne LIKE 'AF{yy}%' ORDER BY code_interne DESC LIMIT 1`. Repart à `AF{yy}001` chaque année, résiste aux suppressions, conforme convention Digiforma — `src/app/api/sessions/route.js`
- 2026-05-08 — [proposé] Atomicité POST /api/inscriptions : valider la session (existence + dates valides + start ≤ end) AVANT toute écriture, puis envelopper INSERT inscription + boucle INSERT émargements dans `db.transaction()`. Empêche les inscriptions Qualiopi orphelines (sans rail d'émargements) en cas de session FK absente, dates invalides ou crash en cours de boucle. Miroir du pattern transactionnel validé pour /api/expenses (27 avr) et /api/projects (2 mai) — `src/app/api/inscriptions/route.js`
- 2026-05-09 — [proposé] Ajouter une UNIQUE INDEX sur `emargements(session_id, apprenant_id, date)` précédée d'un dédoublonnage défensif (UPDATE pour fusionner matin/apres_midi via MAX, puis DELETE rowid NOT IN MIN(rowid)). Rend enfin actif le `INSERT OR IGNORE` du POST /api/inscriptions (actuellement code mort, pas de contrainte ⇒ pas d'IGNORE), garantit le rail Qualiopi "1 ligne par apprenant × session × date" exigé par les indicateurs 11/12, et transforme tout futur doublon silencieux en erreur explicite (SqliteError UNIQUE) — `src/lib/db.mjs`
- 2026-05-13 — [proposé] Éliminer le N+1 dans GET /api/cockpit (calcul `emargementMissing`) : remplacer `SELECT sessions WHERE status='ongoing'` + 2 SELECT COUNT en boucle (inscriptions, emargements) par une seule requête avec deux LEFT JOIN sur sous-agrégats. Passe de `1 + 2N` à `1` requête à chaque chargement de la home Mission Control. Joins couverts par `idx_inscriptions_session_id` et `idx_emargements_session_id` déjà en place. Comportement, payload et filtres date identiques — `src/app/api/cockpit/route.js`
- 2026-05-14 — [proposé] Ajouter PUT/DELETE sur `/api/bank-balances/[id]` (miroir exact des patterns `recurring-costs/[id]` et `forecast-items/[id]`). Permet de corriger un snapshot bancaire typo'd ou purger un compte clôturé — sinon le mauvais montant pollue à vie le `currentBalance` du `GET /api/treasury/forecast` (pris via `MAX(snapshot_date)` par compte) et fausse les projections T+30/60/90 et l'alerte seuil. Zéro changement de schéma, route purement additive — `src/app/api/bank-balances/[id]/route.js` (à créer)
- 2026-05-15 — [proposé] Éliminer le N+1 dans GET /api/cockpit (bloc `resources.team`) : remplacer 3 SELECT COUNT par membre d'équipe (`tasksOpen`, `tasksLate`, `projectsCount`) par 1 seul `SELECT assignee_id, assignee_name, due_date, project_id FROM tasks WHERE status != 'done'` + agrégation JS (`filter` + `new Set`). Passe de `1 + 3N` à `1` requête (31 → 1 pour 10 membres). Comportement strictement identique (`COUNT(DISTINCT project_id)` ↔ `new Set(...).size` côté JS, NULL filtré dans les deux cas). Miroir du pattern validé pour `/api/data` (28 avr) et `emargementMissing` (13 mai) — `src/app/api/cockpit/route.js`
- 2026-05-19 — [proposé] Atomicité de la régénération des émargements dans PATCH /api/sessions/[id] : wrapper le bloc snapshot existants → DELETE FROM emargements WHERE session_id=? → boucle INSERT (apprenants × jours) dans `db.transaction()`. Aujourd'hui un crash entre le DELETE et la fin de la boucle (process killed, hot-reload pendant le PATCH, Date invalide, FK violation, disque plein) détruit irréversiblement tout le rail d'émargement de la session — exactement la preuve Qualiopi exigée par les indicateurs 11/12. Avec la transaction : soit le rail est régénéré complètement, soit les anciens émargements (avec attendance matin/apres_midi déjà cochée) sont conservés intacts. Miroir du pattern transactionnel validé pour /api/expenses (27 avr), /api/projects (2 mai), /api/inscriptions (8 mai), mais sur un flow de modification destructive (DELETE + INSERT loop) au lieu d'un flow de création. Zéro changement de comportement nominal, zéro impact perf (better-sqlite3 transactions sync) — `src/app/api/sessions/[id]/route.js`
- 2026-05-20 — [proposé] Débloquer le type d'évaluation `'froid'` dans POST /api/evaluations : ajouter `'froid'` à la whitelist de validation (`['positionnement','acquis','satisfaction']` → `['positionnement','acquis','satisfaction','froid']`). Le schéma DB l'accepte déjà depuis la migration du CHECK constraint (db.mjs L1099-1119) et le front (formations/page.jsx L7682) propose l'onglet "À froid" + envoie `type: 'froid'`, mais le POST renvoyait silencieusement 400 "type invalide" → aucune évaluation à froid sauvegardable. Show-stopper invisible pour la **preuve indicateur Qualiopi 31** (mesure d'efficacité différée 4-8 semaines post-formation). 1 caractère de diff utile, zéro risque, aligne enfin les 3 couches DB ↔ API ↔ UI — `src/app/api/evaluations/route.js`
- 2026-05-21 — [proposé] Corriger le pas mensuel des coûts récurrents dans GET /api/treasury/forecast : la boucle `for (let m = 0; m < monthsCount; m++)` avance d'1 mois à chaque itération quelle que soit `rc.frequency`, alors que `monthsCount` est déjà divisé par 3 (quarterly) ou 12 (yearly). Conséquence : un coût `quarterly` sur horizon 90j produit 0 occurrence (m=0 seulement → date ce mois → souvent passée donc skip) au lieu de la 1 occurrence attendue ; sur horizon 365j il génère 5 occurrences en 5 mois consécutifs au lieu de 4 trimestrielles. Pareil pour `yearly`. Fix : introduire `stepMonths = 1|3|12` et boucler `for (let m = 0; m <= horizonMonths; m += stepMonths)`. La DB n'a aujourd'hui que des coûts `monthly` (6 actifs : URSSAF, comptable, Adobe, Crédit mutuel, Dropbox, BPI) — bug latent qui se déclenchera dès la 1ère assurance RC Pro annuelle ou taxe trimestrielle. Impact : projections T+30/60/90 et alerte seuil de Mission Control deviennent correctes. ~5 lignes de diff, zéro schéma, zéro API — `src/app/api/treasury/forecast/route.js`
- 2026-05-22 — [proposé] Sécuriser DELETE /api/formateurs/[id] : bloquer la suppression si le formateur a au moins une session liée (`SELECT COUNT(*) FROM sessions WHERE formateur_id = ?`), renvoyer 409 CONFLICT avec message "N sessions liées, passer en statut_collab='inactif' à la place". Aujourd'hui le DELETE est brutal et `sessions.formateur_id TEXT` n'a aucune contrainte FK (db.mjs L532) → orphelins silencieux + perte irréversible de la **preuve Qualiopi indicateur 9** (CV/biographie/qualifications/diplômes/domaines/date_dernier_dev_pro du formateur) pour toutes les sessions passées. Le champ dénormalisé `formateur_name` ne sauve que le nom, pas la qualification exigée par l'auditeur. Miroir du pattern de protection FK utilisé dans DELETE /api/clients/[id] (qui unlinke les projets). Pattern 409 déjà présent dans /api/inscriptions et /api/users. ~8 lignes de diff, additif, zéro schéma — `src/app/api/formateurs/[id]/route.js`
- 2026-05-23 — [proposé] Atomicité POST /api/formations + mapping codes SQLite : wrapper le bloc `SELECT next_index` → `UPDATE next_indices SET next_index = next_index + 1 WHERE pillar = 'GRIOTHEQUE'` → `INSERT INTO formations` dans `db.transaction(() => {...})()`, et enrichir le `catch` avec le mapping `SQLITE_CONSTRAINT_*` → `FK_VIOLATION` / `VALIDATION_ERROR` / `DUPLICATE_ID` (status 400) sinon `SERVER_ERROR` (500), miroir du pattern validé pour /api/expenses (27 avr) et /api/projects (2 mai). Bug RÉEL avec PREUVE en prod : la DB contient aujourd'hui les formations `PR26005, PR26006…PR26015` (11 formations) mais `next_indices.GRIOTHEQUE = 19`, soit **7 codes Digiforma consommés sans formation associée** (`PR26001-004` et `PR26016-018` manquants → trous dans la séquence de numérotation programme). Cause : si l'`INSERT formations` échoue (CHECK constraint, NOT NULL, JSON.stringify d'un payload exotique), le UPDATE next_indices ligne au-dessus est déjà commit → compteur incrémenté pour rien. Avec la transaction, le rollback restaure `next_index`. Impact direct : preuve **Qualiopi indicateur 8** (informations communiquées au public — programme = catalogue numéroté) + cohérence convention Digiforma (numérotation continue PRyyXXX). ~10 lignes de diff, zéro changement de schéma, zéro changement de comportement nominal — `src/app/api/formations/route.js`
- 2026-05-24 — [proposé] Réveiller l'alerte morte `emargementMissing` dans GET /api/cockpit : remplacer `COUNT(*) FROM emargements WHERE session_id=? AND date=?` par `COUNT(*) ... AND (matin = 1 OR apres_midi = 1)`. Bug structurel depuis le jour 1 : POST /api/inscriptions (ligne 62-69) pré-crée tout le rail d'émargements à l'inscription avec `matin=0, apres_midi=0`, donc `emargsToday == inscritsCount` **toujours** → l'alerte rouge "X émargement(s) manquant(s) — Qualiopi" affichée sur Mission Control (page.jsx L249-254) est code mort, elle ne s'est jamais déclenchée. Avec le fix, le COUNT ne mesure plus les lignes existantes mais les présences réellement signées (au moins un demi-jour coché via PATCH /api/emargements). Restaure la visibilité de la **preuve Qualiopi indicateurs 11 (assiduité) et 12 (information continue)** sur la home. ~3 caractères utiles de diff, zéro schéma, zéro perf (couvert par idx_emargements_session_id), orthogonal au N+1 fix proposé le 13 mai (la condition s'intègre dans le LEFT JOIN sous-agrégat) — `src/app/api/cockpit/route.js`
- 2026-05-16 — [proposé] Normaliser POST /api/apprenants en snake_case : renommer ~22 variables destructurées camelCase (`dateNaissance`, `situationPro`, `statutJuridique`, `precisionHandicap`, `niveauExp`, `modalitePaiement`, `connuComment`, `etatRelance`, `orgaOpco`, `statutFinancement`, `financementEntreprise`, `entrepriseAdresse/Cp/Ville/Tel`, `emailReferent`, `nomReferent`, `dossierUrl`, `lienCalendly`, `datePositionnement`, `dateEnveiDoc` [typo!], `dateInscription`) vers snake_case pour matcher le payload front (`ApprenantForm` ligne 663-691) et le PATCH `/[id]` (ligne 33-48). Stoppe la perte silencieuse de ~22 champs Digiforma/Qualiopi à la création (handicap, situation pro, financement OPCO, dates pipeline) — actuellement le front envoie `date_envoi_doc`, le POST destructure `dateEnveiDoc` → défaut `''` → champ vide en base. Zéro changement de schéma, ni d'API, ni de comportement côté front — `src/app/api/apprenants/route.js`
- 2026-05-25 — [proposé] Sécuriser DELETE /api/apprenants/[id] : bloquer la suppression si l'apprenant a au moins une inscription liée (`SELECT COUNT(*) FROM inscriptions WHERE apprenant_id = ?`), renvoyer 409 CONFLICT avec message "N inscription(s) liée(s) — archiver via PATCH etat plutôt que supprimer (preuves Qualiopi en jeu)". Aujourd'hui le DELETE est brutal et les 3 FKs `inscriptions/emargements/evaluations.apprenant_id` ont toutes `ON DELETE CASCADE` (db.mjs L556/569/579, `foreign_keys = ON` confirmé L17) → 1 click détruit IRRÉVERSIBLEMENT le dossier Qualiopi complet de l'apprenant : inscriptions (parcours), émargements (preuve indicateur 11 assiduité), évaluations à chaud + à froid (preuves indicateurs 30/31 efficacité). PREUVE en prod : 19/31 apprenants ont des inscriptions, 3 ont des émargements signés. Miroir exact du pattern proposé pour DELETE /api/formateurs/[id] (22 mai) — même protection 409, sur l'autre côté de la relation session×apprenant. Pattern 409 déjà rodé dans /api/inscriptions et /api/users. ~8 lignes de diff, additif, zéro schéma — `src/app/api/apprenants/[id]/route.js`
