# lesgriots.com — fichiers internes JAMAIS servis directement.
#
# La page publique est UNIQUEMENT index.html, pilotée par le BO
# (« Pages du site » : attente ↔ site complet). Sans ce snippet, un visiteur
# pouvait ouvrir /site.html ou /site.live.html et voir le site complet même
# quand la page d'attente est active.
#
# Inclus depuis les server blocks de lesgriots.com.conf, juste après
# server_name (les locations regex déclarées ICI passent avant celles du
# fichier principal — ordre d'apparition).

location ~* ^/(site|site\.live|attente|archive|boutique|specimens|index\.slideover-backup)\.html$ { return 404; }
location ~* (\.placeholder\.html$|^/_old-|^/apercu-|^/tweaks-panel\.jsx$|^/image-slot\.js$) { return 404; }
